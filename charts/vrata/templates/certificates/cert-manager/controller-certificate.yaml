{{- if and .Values.tls.enabled .Values.tls.certManager.enabled .Values.controller.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "vrata.fullname" . }}-tls-controller
  labels:
    {{- include "vrata.labels" . | nindent 4 }}
spec:
  secretName: {{ include "vrata.tlsControllerSecretName" . }}
  duration: 8760h
  renewBefore: 720h
  isCA: false
  commonName: {{ include "vrata.fullname" . }}-controller
  usages:
    - client auth
    - digital signature
    - key encipherment
  issuerRef:
    name: {{ include "vrata.fullname" . }}-ca
    kind: Issuer
{{- end }}
